# CORAL ASTROBIO v3.0 — Variables de entorno
# Copia este archivo como .env y rellena tus credenciales
# Nunca subas el .env real a git

# ── Supabase ─────────────────────────────────────────────────
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
# Opcional: service key para bypass de RLS
# SUPABASE_SERVICE_KEY=eyJhbGci...

# ── OpenAI (para embeddings) ─────────────────────────────────
# Si no tienes OpenAI, los embeddings se omiten silenciosamente
# La búsqueda semántica hará fallback a búsqueda de texto
OPENAI_API_KEY=sk-...

# ── API Server ───────────────────────────────────────────────
API_PORT=5050
API_HOST=0.0.0.0

# ── Frontend (si sirves el HTML desde otro origen) ───────────
# CORS_ORIGIN=http://localhost:3000
